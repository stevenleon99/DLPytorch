unsupervise machine learning
 - use encode to downsample and decoder to upsample
 - use mse or cross-entropy to compared the original image and the generated image
 - other downsample method: PCA (linear mapping)
 - the autoencoder performs better than PCA which loss a lot of non linear information
 - 